import { Train } from './train';
export class Itenary {
  trainList: Train[];
  totalFare: number;
  displayDate: String;
  email: string;
  booking_id:number;
  bookingStatus:string;
  noOfPassenger: number;
}
